<%- include('partials/header') %>

<div class="page-header">
  <h1>Recherche : "<%= query %>"</h1>
  <p class="text-muted"><%= videos.length %> résultat(s)</p>
</div>

<% if (videos.length > 0) { %>
  <div class="file-list">
    <% for (const video of videos) { %>
      <a href="/videos/<%= video.id %>" class="file-item video-item">
        <span class="file-icon">&#127909;</span>
        <div class="file-info">
          <span class="file-name"><%= video.title || video.filename %></span>
          <span class="file-path"><%= video.library_name %> &mdash; <%= video.filepath %></span>
        </div>
        <span class="file-size"><%= formatSize(video.size) %></span>
      </a>
    <% } %>
  </div>
<% } else { %>
  <div class="empty-state">
    <p>Aucune vidéo trouvée pour "<%= query %>".</p>
  </div>
<% } %>

<%
function formatSize(bytes) {
  if (!bytes) return '—';
  const units = ['o', 'Ko', 'Mo', 'Go', 'To'];
  let i = 0;
  let size = bytes;
  while (size >= 1024 && i < units.length - 1) { size /= 1024; i++; }
  return size.toFixed(i > 0 ? 1 : 0) + ' ' + units[i];
}
%>

<%- include('partials/footer') %>
